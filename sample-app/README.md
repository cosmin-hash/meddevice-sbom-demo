# sample-app (demonstration target)

A tiny Python app with **intentionally outdated dependencies** so the
SBOM + vulnerability + VEX pipeline produces real, non-empty results.

Do not use these dependency versions in production. They exist solely to
demonstrate how findings are generated, triaged (VEX), and documented.
